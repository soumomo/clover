from setuptools import setup, find_packages

setup(
    name="canopy-core",
    version="0.1.0",
    description="Tree Crown Detection & Canopy Area Estimation Platform",
    packages=find_packages(),
    python_requires=">=3.9",
)
